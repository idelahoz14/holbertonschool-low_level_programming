#include "holberton.h"
/**
 *print_square - Prints a square followed by a new line
 *@size: test variable
 *
 *Return: void
 */
void print_square(int size)
{
int index = 0;

do {
int count = 0;

while (count < size)
{
putchar('#');
count++;
}
putchar('\n');
index++;
} while x(index < size);
}
